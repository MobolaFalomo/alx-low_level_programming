#include <stdio.h>

/**
  * main - program prints its name
  * @argc: counts argument
  * @argv: array for all the argument
  * Return: return 0
  */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
