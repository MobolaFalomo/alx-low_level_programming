CC = gcc
SRC = main.c school.c
OBJ = $(SRC:.c=.o)
NAME = school

all: $(NAME)
$(NAME): $(OBJ)
    $(CC) -o $@ $(OBJ)

# Rule to compile individual source files into object files
%.o: %.c
    $(CC) -c -o $@ $<

# Cleanup rule
clean:
    rm -f $(OBJ) $(NAME)

